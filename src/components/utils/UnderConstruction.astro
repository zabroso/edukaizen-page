---
const {
  title = "Página en construcción",
  subtitle = "Estamos trabajando para ofrecerte esta sección muy pronto.",
  showBack = true,
} = Astro.props;
---

<section
  class="relative flex min-h-[70vh] items-center justify-center overflow-hidden px-6"
>
  <!-- Fondo decorativo -->
  <div class="absolute inset-0 -z-10">
    <div
      class="absolute left-1/2 top-1/4 h-72 w-72 -translate-x-1/2 rounded-full bg-orange-500/20 blur-3xl"
    >
    </div>
    <div
      class="absolute right-1/4 bottom-1/4 h-64 w-64 rounded-full bg-orange-300/10 blur-3xl"
    >
    </div>
  </div>

  <!-- Contenido -->
  <div
    class="max-w-md rounded-2xl border border-border bg-background/70 p-10 text-center backdrop-blur"
  >
    <!-- Ícono -->
    <div
      class="mx-auto mb-6 flex h-16 w-16 items-center justify-center rounded-full bg-orange-500/10 text-orange-500"
    >
      🚧
    </div>

    <h1 class="text-2xl font-bold tracking-tight text-foreground">
      {title}
    </h1>

    <p class="mt-3 text-sm text-foreground/70">
      {subtitle}
    </p>

    {
      showBack && (
        <div class="mt-6">
          <a
            href="/"
            class="inline-flex items-center justify-center rounded-lg bg-orange-500 px-5 py-2.5 text-sm font-medium text-white transition hover:bg-orange-600"
          >
            Volver al inicio
          </a>
        </div>
      )
    }
  </div>
</section>
