---
// Sidebar.astro
const navItems = [
  { label: "Educación", href: "#top" },
  { label: "Conceptos", href: "#conceptos" },
  { label: "Blog", href: "#blog" },
  { label: "Contacto", href: "#contacto" },
];
---

<aside class="hidden w-64 shrink-0 md:block">
  <nav
    class="sticky top-24 rounded-xl border border-border bg-background/60 p-6 backdrop-blur"
  >
    <ul class="space-y-4">
      {
        navItems.map((item) => (
          <li>
            <a
              href={item.href}
              class="block rounded-lg px-3 py-2 text-sm font-medium text-foreground/80 transition hover:bg-foreground/5 hover:text-foreground"
            >
              {item.label}
            </a>
          </li>
        ))
      }
    </ul>
  </nav>
</aside>
