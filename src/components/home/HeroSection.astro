---
import eduIcon from "@/assets/edu.svg?raw";
import kaiIcon from "@/assets/kai.svg?raw";
import zenIcon from "@/assets/zen.svg?raw";

const heroIcons = [
  { icon: eduIcon, title: "EDU", href:"/edu" },
  { icon: kaiIcon, title: "KAI", href:"/kai" },
  { icon: zenIcon, title: "ZEN", href:"/zen" },
];
---

<section
  id="home"
  class="relative flex min-h-screen items-center justify-center overflow-hidden py-10 lg:py-32"
>
  <div class="container mx-auto px-4 text-center">
    <div class="mt-16 grid grid-cols-1 gap-12 md:grid-cols-3">
      {heroIcons.map((item) => (
        <a
          href={item.href}
          class="group relative flex aspect-square items-center justify-center"
        >
          <!-- Overlay -->
          <div
            class="absolute inset-0 bg-black/50 opacity-0 backdrop-blur-sm transition-all duration-300 group-hover:opacity-100"
          ></div>

          <!-- Icon -->
          <div
            class="
              relative z-10
              text-primary
              transition-all duration-300
              group-hover:scale-90
              group-hover:opacity-20
              group-hover:blur-sm

              w-40 h-40
              md:w-80 md:h-80  /* 👈 tamaño original en desktop */
            "
          >
            <span
              class="[&>svg]:w-full [&>svg]:h-full [&>svg]:max-w-none"
              set:html={item.icon}
            />
          </div>

          <!-- Hover text -->
          <div
            class="absolute inset-0 flex flex-col items-center justify-center opacity-0 transition-opacity duration-300 group-hover:opacity-100"
          >
            <span class="font-headline text-2xl font-bold tracking-wider text-foreground">
              {item.title}
            </span>
            <span class="mt-2 text-lg font-semibold text-primary underline underline-offset-4">
              go to blog
            </span>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>